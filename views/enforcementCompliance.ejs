<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Enforcement & Compliance Logging</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="#">FMO Admin</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/dashboard/admin">Dashboard</a>
          </li>
          <!-- Registration Dropdown -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownRegister" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Register
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownRegister">
              <li><a class="dropdown-item" href="/register/fisherfolk">Fisherfolk Registration</a></li>
              <li><a class="dropdown-item" href="/aquaculture/register">Aquaculture Facility Registration</a></li>
              <li><a class="dropdown-item" href="/logs">Audit Logs & Activity Tracking</a></li>
            </ul>
          </li>

          <!-- Reports & Tracking Dropdown -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownReports" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Reports & Tracking
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownReports">
              <li><a class="dropdown-item" href="/catch-report/review">Catch Report Review</a></li>
              <li><a class="dropdown-item" href="/fishing-activity-tracking">Fishing Activity Tracking</a></li>
              <li><a class="dropdown-item" href="/enforcement-compliance-logging">Enforcement and Compliance Logging</a></li>
              <li><a class="dropdown-item" href="/violation-notification-system">Violation Notification System</a></li>
            </ul>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="#">Welcome, <%= user.name %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#logoutModal">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

 <!-- Logout Confirmation Modal -->
 <div class="modal fade" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="logoutModalLabel">Confirm Logout</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Are you sure you want to log out?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <a href="/auth/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>
  </div>
</div>

  <div class="container mt-4">
    <h1>Enforcement & Compliance Logging</h1>
    <!-- Filter and Sorting Form -->
    <form method="GET" action="/enforcement-compliance-logging" class="mb-4">
      <div class="row">
        <div class="col-md-4">
          <label for="incident" class="form-label">Incident</label>
          <input type="text" id="incident" name="incident" class="form-control" placeholder="Enter incident" value="<%= filters.incident %>">
        </div>
        <div class="col-md-4">
          <label for="violation" class="form-label">Violation</label>
          <input type="text" id="violation" name="violation" class="form-control" placeholder="Enter violation" value="<%= filters.violation %>">
        </div>
        <div class="col-md-4">
          <label for="date" class="form-label">Date</label>
          <input type="date" id="date" name="date" class="form-control" value="<%= filters.date %>">
        </div>
      </div>
      <div class="row mt-3">
        <div class="col-md-4">
          <label for="sortBy" class="form-label">Sort By</label>
          <select id="sortBy" name="sortBy" class="form-select">
            <option value="">None</option>
            <option value="incident" <%= sortBy === 'incident' ? 'selected' : '' %>>Incident</option>
            <option value="violation" <%= sortBy === 'violation' ? 'selected' : '' %>>Violation</option>
            <option value="date" <%= sortBy === 'date' ? 'selected' : '' %>>Date</option>
          </select>
        </div>
        <div class="col-md-4 align-self-end">
          <button type="submit" class="btn btn-primary">Apply Filters</button>
        </div>
      </div>
    </form>
    <!-- Logs Table -->
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Incident</th>
          <th>Violation</th>
          <th>Penalty</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% if (logs.length) { %>
          <% logs.forEach(function(log) { %>
            <tr>
              <td><%= log.incident %></td>
              <td><%= log.violation %></td>
              <td><%= log.penalty %></td>
              <td><%= log.date %></td>
            </tr>
          <% }); %>
        <% } else { %>
          <tr>
            <td colspan="4" class="text-center">No logs found.</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
