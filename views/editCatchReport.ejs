<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Catch Report - Fisheries Management</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

  <style>
    :root {
      --primary: #1e3a8a;
      --accent: #3b82f6;
      --background: #f0f4ff;
      --card-bg: #ffffff;
      --text: #1f2937;
      --border: #d1d5db;
      --highlight: #10b981;
      --warning: #ef4444;
    }

    body {
      font-family: 'Poppins', Arial, Helvetica, sans-serif;
      background-color: var(--background);
      color: var(--text);
      margin: 0;
      padding: 0;
      overflow-x: hidden;
    }

    /* Navbar */
    .navbar {
      background-color: var(--primary);
      padding: 1rem 1.5rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .navbar-brand {
      font-weight: 600;
      font-size: 1.5rem;
      color: #fff !important;
    }
    .navbar .nav-link { color: #fff !important; }

    /* Sidebar */
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      width: 250px;
      height: 100%;
      background-color: var(--card-bg);
      padding-top: 1rem;
      overflow-y: auto;
      box-shadow: 2px 0 5px rgba(0,0,0,0.1);
      transition: transform 0.3s ease;
      z-index: 1000;
    }
    .sidebar-logo {
      border-bottom: 1px solid rgba(0,0,0,0.1);
      margin-bottom: 1rem;
      text-align: center;
    }
    .sidebar-logo img {
      max-width: 100%;
      height: auto;
    }
    .sidebar .nav-link {
      color: var(--text);
      padding: 0.75rem 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-weight: 500;
      border-radius: 6px;
      text-decoration: none;
    }
    .sidebar .nav-link:hover { background-color: rgba(59, 130, 246, 0.1); color: var(--primary); }
    .sidebar .sub-item { padding-left: 2.5rem; font-size: 0.9rem; color: var(--text); }
    .sidebar .sub-item:hover { color: var(--primary); text-decoration: none; }

    body { margin-left: 250px; }
    @media (max-width: 991px) {
      .sidebar { transform: translateX(-100%); }
      .sidebar.active { transform: translateX(0); }
      body { margin-left: 0; }
    }

    /* Main Container */
    .main-container { max-width: 1600px; margin: 2rem auto; padding: 0 1.5rem; }

    .section-header {
      font-size: 1.4rem;
      font-weight: 500;
      color: var(--primary);
      margin: 2rem 0 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .section-header i { color: var(--accent); }

    .form-container {
      background-color: var(--card-bg);
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.05);
      padding: 1.5rem;
      max-width: 800px;
      margin: 0 auto;
    }
    .form-label { font-size: 0.95rem; font-weight: 500; color: var(--primary); }
    .form-control, .form-select { padding: 0.75rem 1rem; border: 1px solid var(--border); border-radius: 8px; background: #f9fafb; }

    button[type="submit"] {
      padding: 0.75rem 1.5rem;
      background-color: var(--accent);
      border: none;
      border-radius: 8px;
      color: #fff;
      font-weight: 500;
    }

    .locked-message {
      padding: 1rem;
      background: #fee2e2;
      border: 1px solid #fca5a5;
      color: #b91c1c;
      border-radius: 8px;
      margin-bottom: 1rem;
      font-weight: 500;
    }
  </style>
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg">
  <div class="container-fluid justify-content-between">
    <button class="btn btn-light d-lg-none" id="sidebarToggle"><i class="fas fa-bars"></i></button>
    <a class="navbar-brand" href="/">Fisheries Management Office</a>
    <ul class="navbar-nav flex-row align-items-center">
      <li class="nav-item me-3">
        <a class="nav-link"><i class="fas fa-user me-1"></i> Welcome, <%= user.name %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" data-bs-toggle="modal" data-bs-target="#logoutModal">
          <i class="fas fa-sign-out-alt me-1"></i> Logout
        </a>
      </li>
    </ul>
  </div>
</nav>

<!-- Sidebar -->
<div class="sidebar">
  <div class="sidebar-logo py-3">
    <a href="/dashboard/admin">
      <img src="/image/lugu.jpg" alt="Fisheries Management Office Logo" class="img-fluid" style="max-height: 100px;">
    </a>
  </div>
  <ul class="nav flex-column">
    <li class="nav-item">
      <a class="nav-link" href="/"><i class="fas fa-tachometer-alt me-2"></i> Dashboard</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" data-bs-toggle="collapse" href="#userTools" role="button" aria-expanded="false">
        <i class="fas fa-tools me-2"></i> User Tools
      </a>
      <div class="collapse" id="userTools">
        <ul class="list-unstyled ps-3">
          <li><a class="nav-link sub-item" href="/climateLoss/view"><i class="fas fa-cloud-sun me-2"></i> Post-Climate Event Loss Reporting</a></li>
          <li><a class="nav-link sub-item" href="/climateLoss/view/reports"><i class="fas fa-table me-2"></i> View Loss Reports</a></li>
          <li><a class="nav-link sub-item" href="/fishing-activity-list"><i class="fas fa-anchor me-2"></i> Fishing Activity Table</a></li>
          <li><a class="nav-link sub-item" href="/catch-report/predictions"><i class="fas fa-clock me-2"></i> Predict Best Catch Time</a></li>
          <li><a class="nav-link sub-item" href="/catch-report"><i class="fas fa-fish me-2"></i> Report Daily Catch</a></li>
          <li><a class="nav-link sub-item" href="/fishing-activity-tracking"><i class="fas fa-anchor me-2"></i> Log Fishing Activity</a></li>
          <li><a class="nav-link sub-item" href="/harvest/user-view"><i class="fas fa-chart-line me-2"></i> Harvest</a></li>
          <li><a class="nav-link sub-item" href="/harvest/add"><i class="fas fa-plus me-2"></i> Add Harvest</a></li>
          <li><a class="nav-link sub-item" href="/violation-history"><i class="fas fa-exclamation-triangle me-2"></i> My Violations</a></li>
          <li><a class="nav-link sub-item" href="/catch-report/history"><i class="fas fa-exclamation-triangle me-2"></i> My Catch History</a></li>
          <li><a class="nav-link sub-item" href="/catch-report/clusters"><i class="fas fa-map-marker-alt me-2"></i> View Clusters</a></li>
        </ul>
      </div>
    </li>
  </ul>
</div>

<!-- Logout Modal -->
<div class="modal fade" id="logoutModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Confirm Logout</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">Are you sure you want to log out?</div>
      <div class="modal-footer">
        <button class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <a href="/auth/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>
  </div>
</div>

<!-- Main Container -->
<div class="main-container">
  <div class="section-header"><i class="fas fa-pen"></i> Edit Catch Report</div>

  <div class="form-container">

    <% if (report.status === "approved") { %>
      <div class="locked-message">This report has already been approved and can no longer be edited.</div>
    <% } %>

<form action="/catch-report/edit/update" method="POST">
  <input type="hidden" name="reportId" value="<%= report.id %>">

  <div class="mb-3">
    <label class="form-label">Species</label>
    <input type="text" class="form-control" name="species" value="<%= report.species %>" <%= report.status === "approved" ? "disabled" : "" %>>
  </div>

  <div class="mb-3">
    <label class="form-label">Quantity</label>
    <input type="number" class="form-control" name="quantity" value="<%= report.quantity %>" <%= report.status === "approved" ? "disabled" : "" %>>
  </div>

  <div class="mb-3">
    <label class="form-label">Location</label>
    <input type="text" class="form-control" name="location" value="<%= report.location %>" <%= report.status === "approved" ? "disabled" : "" %>>
  </div>

  <div class="mb-3">
    <label class="form-label">Method of Fishing</label>
    <input type="text" class="form-control" name="method_of_fishing" value="<%= report.method_of_fishing %>" <%= report.status === "approved" ? "disabled" : "" %>>
  </div>

  <div class="mb-3">
    <label class="form-label">Date</label>
    <input type="date" class="form-control" name="date" value="<%= report.date.toISOString().split('T')[0] %>" <%= report.status === "approved" ? "disabled" : "" %>>
  </div>

  <% if (report.status !== "approved") { %>
    <button type="submit" class="btn btn-success">Save Changes</button>
  <% } %>
</form>


  </div>
</div>

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
<script>
document.addEventListener("DOMContentLoaded", function() {
  const sidebarToggle = document.getElementById("sidebarToggle");
  const sidebar = document.querySelector(".sidebar");

  sidebarToggle.addEventListener("click", () => sidebar.classList.toggle("active"));

  // Close sidebar on mobile when clicking outside
  document.addEventListener("click", function(e) {
    if (window.innerWidth <= 991 && !sidebar.contains(e.target) && !sidebarToggle.contains(e.target)) {
      sidebar.classList.remove("active");
    }
  });
});
</script>

</body>
</html>
