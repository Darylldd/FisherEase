<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aquaculture Facilities</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --primary: #1e3a8a; /* Deep navy */
            --accent: #3b82f6; /* Vibrant blue */
            --accent-light: #60a5fa; /* Lighter blue for subtle button */
            --background: #f0f4ff; /* Light blue-gray */
            --card-bg: #ffffff;
            --text: #1f2937;
            --border: #d1d5db;
            --highlight: #10b981; /* Green for success */
            --warning: #ef4444; /* Red for alerts */
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: var(--background);
            color: var(--text);
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        /* Navbar */
        .navbar {
            background-color: var(--primary);
            padding: 1rem 1.5rem;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .navbar-brand {
            font-weight: 600;
            font-size: 1.5rem;
            color: #fff !important;
        }
        .navbar .nav-link {
            color: #fff !important;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            top: 0px;
            left: 0;
            width: 250px;
            height: 100%;
            background-color: var(--card-bg);
            padding-top: 1rem;
            overflow-y: auto;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .sidebar-logo img {
            max-width: 100%;
            height: auto;
        }
        .sidebar-logo {
            border-bottom: 1px solid rgba(0,0,0,0.1);
            margin-bottom: 1rem;
        }

        .sidebar .nav-link {
            color: var(--text);
            padding: 0.75rem 1rem;
        }
        .sidebar .sub-item {
            padding-left: 2.5rem;
            font-size: 0.9rem;
        }

        /* Default visible on desktop */
        body {
            margin-left: 250px;
        }

        /* Hide sidebar on mobile */
        @media (max-width: 991px) {
            .sidebar {
                transform: translateX(-100%);
            }
            body {
                margin-left: 0;
            }
            .sidebar.active {
                transform: translateX(0);
                z-index: 1050;
            }
        }

        /* Main Container */
        .main-container {
            max-width: 1200px; /* Wider for table */
            margin: 2rem auto;
            padding: 0 1.5rem;
        }

        /* Form Section */
        .form-section {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        .form-section:hover {
            transform: translateY(-4px);
        }
        .form-section h2 {
            font-size: 1.8rem;
            font-weight: 600;
            color: var(--primary);
            margin-bottom: 1.5rem;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        .form-section h2 i {
            color: var(--accent);
        }

        /* Table */
        .table-responsive {
            margin-top: 1.5rem;
        }
        .table {
            background-color: var(--card-bg);
            border-radius: 8px;
            overflow: hidden;
        }
        .table th {
            background-color: var(--primary);
            color: #ffffff;
            font-weight: 500;
            font-size: 0.95rem;
            padding: 1rem;
        }
        .table td {
            font-size: 0.9rem;
            padding: 1rem;
            vertical-align: middle;
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(59, 130, 246, 0.03);
        }
        .table-hover tbody tr:hover {
            background-color: var(--background);
        }

        /* Pagination */
        .pagination {
            justify-content: center;
            margin-top: 1.5rem;
        }
        .page-link {
            border-radius: 6px;
            margin: 0 0.2rem;
            color: var(--text);
            border: 1px solid var(--border);
            transition: all 0.3s ease;
        }
        .page-link:hover {
            background-color: var(--accent);
            color: #ffffff;
            border-color: var(--accent);
        }
        .page-item.active .page-link {
            background-color: var(--accent);
            border-color: var(--accent);
            color: #ffffff;
        }
        .page-item.disabled .page-link {
            color: #9ca3af;
            background-color: var(--card-bg);
            border-color: var(--border);
        }

        /* Buttons */
        .btn-primary {
            background-color: var(--accent);
            border: none;
            border-radius: 6px;
            font-weight: 500;
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
            color: #ffffff;
            transition: background-color 0.3s ease, transform 0.2s ease;
            text-align: center;
            display: inline-block;
        }
        .btn-primary:hover {
            background-color: #2563eb;
            transform: translateY(-2px);
        }
        .btn-warning {
            background-color: #f59e0b;
            border: none;
            border-radius: 6px;
            font-weight: 500;
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
            color: #ffffff;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }
        .btn-warning:hover {
            background-color: #d97706;
            transform: translateY(-2px);
        }
        .btn-danger {
            background-color: var(--warning);
            border: none;
            border-radius: 6px;
            font-weight: 500;
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
            color: #ffffff;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }
        .btn-danger:hover {
            background-color: #dc2626;
            transform: translateY(-2px);
        }

        /* Modal */
        .modal-content {
            border-radius: 12px;
            border: none;
            overflow: hidden;
        }
        .modal-header {
            background-color: var(--primary);
            color: #ffffff;
            border-bottom: none;
            padding: 1rem 1.5rem;
        }
        .modal-body {
            padding: 1.5rem;
        }
        .modal-footer {
            border-top: none;
            padding: 1rem 1.5rem;
        }
        .btn-secondary {
            background-color: #6b7280;
            border: none;
            border-radius: 6px;
            font-weight: 500;
        }
        .btn-secondary:hover {
            background-color: #4b5563;
        }

        /* Print Styles */
        @media print {
            body * {
                visibility: hidden;
            }
            .table-responsive, .table-responsive * {
                visibility: visible;
            }
            .table-responsive {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
            }
            .table-responsive table {
                font-size: 12pt;
                border-collapse: collapse;
            }
            .table-responsive th, .table-responsive td {
                border: 1px solid #000; /* Simple black border for print */
                padding: 0.5rem;
                background: none !important; /* Remove background colors */
                color: #000 !important; /* Ensure text is black */
            }
            .table-striped tbody tr:nth-of-type(odd) {
                background: none !important; /* Remove striped background */
            }
            .table-hover tbody tr:hover {
                background: none !important; /* Remove hover effect */
            }
            .table-responsive th:nth-child(8), .table-responsive td:nth-child(8) {
                display: none; /* Hide Actions column */
            }
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .main-container {
                padding: 0 1rem;
            }
        }
        @media (max-width: 768px) {
            .navbar-brand {
                font-size: 1.3rem;
            }
            .nav-link {
                font-size: 0.9rem;
                padding: 0.5rem 0.75rem;
            }
            .form-section h2 {
                font-size: 1.5rem;
            }
            .form-section {
                padding: 1.5rem;
            }
            .table th, .table td {
                font-size: 0.85rem;
                padding: 0.75rem;
            }
        }
        @media (max-width: 576px) {
            .main-container {
                margin: 1rem auto;
            }
            .form-section {
                padding: 1rem;
            }
            .form-section h2 {
                font-size: 1.3rem;
            }
            .table th, .table td {
                font-size: 0.8rem;
                padding: 0.5rem;
            }
            .btn-primary, .btn-warning, .btn-danger {
                font-size: 0.85rem;
                padding: 0.4rem 0.8rem;
            }
        }
    </style>
</head>
<body>
    <!-- Top Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid justify-content-between">
            <!-- Sidebar Toggle (Hamburger) -->
            <button class="btn btn-light d-lg-none" id="sidebarToggle">
                <i class="fas fa-bars"></i>
            </button>

            <!-- Brand -->
            <a class="navbar-brand ms-2" href="#">Fisheries Management Office</a>

            <!-- Right side (Welcome + Logout + Print) -->
            <ul class="navbar-nav flex-row align-items-center">
              
                <li class="nav-item me-3">
                    <a class="nav-link" href="#"><i class="fas fa-user me-1"></i> Welcome, <%= user.name %></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#logoutModal">
                        <i class="fas fa-sign-out-alt me-1"></i> Logout
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <!-- Sidebar Logo / Branding -->
        <div class="sidebar-logo text-center py-3">
            <a href="/dashboard/admin">
                <img src="/image/lugu.jpg" alt="Fisheries Management Office Logo" class="img-fluid" style="max-height: 100px;">
            </a>
        </div>

        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link" href="/dashboard/admin"><i class="fas fa-tachometer-alt me-2"></i> Dashboard</a>
            </li>

            <!-- Register -->
            <li class="nav-item">
                <a class="nav-link collapsed" data-bs-toggle="collapse" href="#registerMenu">
                    <i class="fas fa-user-plus me-2"></i> Register
                </a>
                <div class="collapse" id="registerMenu">
                    <a class="nav-link sub-item" href="/register/fisherfolk"><i class="fas fa-fish me-2"></i> Fisherfolk Registration</a>
                    <a class="nav-link sub-item" href="/aquaculture/register"><i class="fas fa-water me-2"></i> Aquaculture Facility Registration</a>
                    <a class="nav-link sub-item" href="/aquaculture/list"><i class="fas fa-table"></i> Aquaculture Facility Table</a>
                    <a class="nav-link sub-item" href="/register/post-harvest"><i class="fas fa-fish "></i> Register Post Harvest</a>
                    <a class="nav-link sub-item" href="/admin/post-harvest"><i class="fas fa-table"></i> Post Harvest Table</a>
                </div>
            </li>

            <!-- Reports -->
            <li class="nav-item">
                <a class="nav-link collapsed" data-bs-toggle="collapse" href="#reportsMenu">
                    <i class="fas fa-file-alt me-2"></i> Reports & Tracking
                </a>
                <div class="collapse" id="reportsMenu">
                    <a class="nav-link sub-item" href="/catch-report/review"><i class="fas fa-clipboard-check me-2"></i> Catch Report Review</a>
                    <a class="nav-link sub-item" href="/fishing-activity"><i class="fas fa-anchor me-2"></i> Fishing Activity Tracking</a>
                    <a class="nav-link sub-item" href="/fishing-vessel-registration"><i class="fas fa-gavel me-2"></i> Enforcement and Compliance</a>
                    <a class="nav-link sub-item" href="/fishing-vessel-registrations"><i class="fas fa-gavel me-2"></i> Vessel Table</a>
                    <a class="nav-link sub-item" href="/violation-notifications"><i class="fas fa-exclamation-triangle me-2"></i> Violation Notifications</a>
                    <a class="nav-link sub-item" href="/violations-table"><i class="fas fa-exclamation-triangle me-2"></i> View Violations</a>
                    <a class="nav-link sub-item" href="/harvest/admin-view"><i class="fas fa-chart-line me-2"></i> Harvest & Production Data</a>
                    <a class="nav-link sub-item" href="/fisherfolk/table"><i class="fas fa-table me-2"></i> Fisherfolk Table</a>
                    <a class="nav-link sub-item" href="/admeasurement-form"><i class="fas fa-table me-2"></i> Admeasurement</a>
                    <a class="nav-link sub-item" href="/admeasurement-forms"><i class="fas fa-table me-2"></i> Admeasurement List</a>
                </div>
            </li>

            <!-- Tools -->
            <li class="nav-item">
                <a class="nav-link collapsed" data-bs-toggle="collapse" href="#toolsMenu">
                    <i class="fas fa-tools me-2"></i> Tools
                </a>
                <div class="collapse" id="toolsMenu">
                    <a class="nav-link sub-item" href="/fish-species"><i class="fas fa-fish me-2"></i> Fish Species Guide</a>
                    <a class="nav-link sub-item" href="/admin/fish-species/add"><i class="fas fa-plus me-2"></i> Add Fish Species</a>
                    <a class="nav-link sub-item" href="/audit/logs"><i class="fas fa-history me-2"></i> Audit Logs & Activity</a>
                    <a class="nav-link sub-item" href="/climateLoss/admin/analysis"><i class="fas fa-cloud-sun me-2"></i> Climate Event Analysis</a>
                </div>
            </li>
        </ul>
    </div>

    <!-- Logout Modal -->
    <div class="modal fade" id="logoutModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirm Logout</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">Are you sure you want to log out?</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <a href="/auth/logout" class="btn btn-danger">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Aquaculture Facilities Table -->
    <div class="main-container">
        <div class="form-section">
            <h2><i class="fas fa-water"></i> Registered Aquaculture Facilities</h2>
            <div class="mb-3">
                <a href="/aquaculture/register" class="btn btn-primary"><i class="fas fa-plus me-2"></i> Add New Facility</a>

             <button onclick="window.print()" class="btn btn-secondary"><i class="fas fa-print me-2"></i> Print Table</button>
            
            </div>
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Facility Name</th>
                            <th>Owner</th>
                            <th>Contact Info</th>
                            <th>Location</th>
                            <th>Fish Species</th>
                            <th>Production Capacity</th>
                            <th>Pond Size (sqm)</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (facilities && facilities.length > 0) { %>
                            <% facilities.forEach(facility => { %>
                                <tr>
                                    <td><%= facility.facility_name %></td>
                                    <td><%= facility.owner %></td>
                                    <td><%= facility.contact_info %></td>
                                    <td><%= facility.location %></td>
                                    <td><%= facility.species %></td>
                                    <td><%= facility.capacity %></td>
                                    <td><%= facility.pond_size %></td>
                                    <td>
                                        <div class="dropdown">
                                            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                                <i class="fas fa-cog"></i>
                                            </button>
                                            <ul class="dropdown-menu">
                                                <li>
                                                    <a href="/aquaculture/edit/<%= facility.id %>" class="dropdown-item">
                                                        <i class="fas fa-edit me-1"></i> Edit
                                                    </a>
                                                </li>
                                                <li>
                                                    <a href="/aquaculture/delete/<%= facility.id %>" class="dropdown-item text-danger" onclick="return confirm('Are you sure you want to delete this facility?')">
                                                        <i class="fas fa-trash me-1"></i> Delete
                                                    </a>
                                                </li>
                                            </ul>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="8" class="text-center-doselect">No aquaculture facilities registered yet.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <% if (totalPages > 1) { %>
                <nav aria-label="Page navigation">
                    <ul class="pagination">
                        <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                            <a class="page-link" href="/aquaculture/list?page=<%= currentPage - 1 %>" aria-label="Previous">
                                <span aria-hidden="true">«</span>
                            </a>
                        </li>
                        <% for (let i = 1; i <= totalPages; i++) { %>
                            <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                                <a class="page-link" href="/aquaculture/list?page=<%= i %>"><%= i %></a>
                            </li>
                        <% } %>
                        <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                            <a class="page-link" href="/aquaculture/list?page=<%= currentPage + 1 %>" aria-label="Next">
                                <span aria-hidden="true">»</span>
                            </a>
                        </li>
                    </ul>
                </nav>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const sidebarToggle = document.getElementById("sidebarToggle");
            const sidebar = document.querySelector(".sidebar");

            sidebarToggle.addEventListener("click", function() {
                sidebar.classList.toggle("active");
            });

            // Optional: Close sidebar when clicking outside (on mobile/tablet)
            document.addEventListener("click", function(e) {
                if (window.innerWidth <= 991 && 
                    !sidebar.contains(e.target) && 
                    !sidebarToggle.contains(e.target)) {
                    sidebar.classList.remove("active");
                }
            });
        });
    </script>
</body>
</html>